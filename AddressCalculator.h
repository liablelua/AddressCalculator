#include <iostring>
#include <iomanip>

class ac {
public:
  long long subtract(unsigned long long address1, unsigned long long address2);
  long long add(unsigned long long address1, unsigned long long address2);
};
